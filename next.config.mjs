/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['tailus.io','via.placeholder.com','mdbcdn.b-cdn.net',"pagedone.io","cdn.rareblocks.xyz"],
      },
};

export default nextConfig;
